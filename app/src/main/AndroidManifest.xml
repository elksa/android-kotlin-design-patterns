<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elksa.design.patterns">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.GofProxyActivity"/>
        <activity android:name=".activities.GofDecoratorActivity" />
        <activity android:name=".activities.GofSingletonActivity" />
        <activity android:name=".activities.GofObserverActivity" />
        <activity android:name=".activities.GofMementoActivity" />
        <activity android:name=".activities.GofCommandActivity" />
        <activity android:name=".activities.GofFacadeActivity" />
        <activity android:name=".activities.GofBuilderActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.GofTemplateMethodActivity" />
    </application>

</manifest>